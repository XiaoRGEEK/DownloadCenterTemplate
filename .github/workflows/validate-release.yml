name: Validate release control plane

on:
  pull_request:
  push:
    branches: ["master"]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Validate release manifests
        run: python3 scripts/validate_release.py
