# Release manifests

Files in this directory are public, immutable declarations produced by
`scripts/prepare_release.py`. They contain only a GitHub Release tag, public
asset names, TOS object keys, byte sizes, and SHA256 digests.

Never add local paths, credentials, tokens, private URLs, customer data, or
certificate material here.
